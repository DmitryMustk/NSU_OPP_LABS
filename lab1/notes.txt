Ускорение: Sp = T1 / Tp, где T1 - время работы ПОСЛЕДОВАТЕЛЬНОЙ программы. Tp
- время работы параллельной программы на p процессах/потоках.
Эффективность: Ep = Sp / p * 100%

MPI_Init(&argc, &argv):

Действие: Инициализирует MPI-окружение.
Принимает: argc и argv — количество аргументов командной строки и массив аргументов командной строки соответственно.
Возвращает: Ничего.

MPI_Finalize():

Действие: Завершает MPI-программу.
Принимает: Ничего.
Возвращает: Ничего.

MPI_Comm_rank(MPI_COMM_WORLD, &rank):

Действие: Определяет ранг текущего процесса в коммуникаторе MPI_COMM_WORLD.
Принимает: MPI_COMM_WORLD — коммуникатор, и адрес переменной rank, куда будет записан ранг текущего процесса.
Возвращает: rank — ранг текущего процесса в коммуникаторе.

MPI_Comm_size(MPI_COMM_WORLD, &size):

Действие: Определяет общее количество процессов в коммуникаторе MPI_COMM_WORLD.
Принимает: MPI_COMM_WORLD — коммуникатор, и адрес переменной size, куда будет записано общее количество процессов.
Возвращает: size — общее количество процессов в коммуникаторе.

MPI_Send(&buf, count, datatype, dest, tag, comm):

Действие: Отправляет сообщение другому процессу.
Принимает: Буфер, количество элементов, тип данных, ранг процесса-получателя, тег сообщения, коммуникатор.
Возвращает: Ничего.

MPI_Recv(&buf, count, datatype, source, tag, comm, &status):

Действие: Принимает сообщение от другого процесса.
Принимает: Буфер, количество элементов, тип данных, ранг процесса-отправителя, тег сообщения, коммуникатор, статус операции.
Возвращает: Ничего.

MPI_Bcast(&buf, count, datatype, root, comm):

Действие: Рассылает данные от одного процесса всем остальным в коммуникаторе.
Принимает: Буфер, количество элементов, тип данных, ранг процесса-рассылателя (root), коммуникатор.
Возвращает: Ничего.

MPI_Reduce(&sendbuf, &recvbuf, count, datatype, op, root, comm):

Действие: Сводит значения, переданные из всех процессов, в одно значение на одном из процессов.
Принимает: Локальный буфер отправки, буфер приема, количество элементов, тип данных, операцию свертки, ранг процесса-корня, коммуникатор.
Возвращает: Ничего.

MPI_Allreduce(&sendbuf, &recvbuf, count, datatype, op, comm):

Действие: Сводит значения, переданные из всех процессов, в одно значение на всех процессах.
Принимает: Локальный буфер отправки, буфер приема, количество элементов, тип данных, операцию свертки, коммуникатор.
Возвращает: Ничего.

MPI_Scatter(&sendbuf, sendcount, sendtype, &recvbuf, recvcount, recvtype, root, comm):

Действие: Разбрасывает данные от одного процесса всем остальным в коммуникаторе.
Принимает: Локальный буфер отправки, количество элементов для отправки каждому процессу, тип данных отправки, буфер приема для каждого процесса, количество элементов для приема, тип данных приема, ранг процесса-рассылателя (root), коммуникатор.
Возвращает: Ничего.

MPI_Gather(&sendbuf, sendcount, sendtype, &recvbuf, recvcount, recvtype, root, comm):

Действие: Собирает данные от всех процессов на один процесс.
Принимает: Локальный буфер отправки, количество элементов для отправки каждым процессом, тип данных отправки, буфер приема для процесса-корня, количество элементов для приема, тип данных приема, ранг процесса-корня, коммуникатор.
Возвращает: Ничего.

MPI_Allgather(&sendbuf, sendcount, sendtype, &recvbuf, recvcount, recvtype, comm):

Действие: Собирает данные от всех процессов на все процессы.
Принимает: Локальный буфер отправки, количество элементов для отправки каждым процессом, тип данных отправки, буфер приема для всех процессов, количество элементов для приема каждым процессом, тип данных приема, коммуникатор.
Возвращает: Ничего.

MPI_Scatterv(&sendbuf, sendcounts, displs, sendtype, &recvbuf, recvcount, recvtype, root, comm):

Действие: Разбрасывает блоки данных от одного процесса всем остальным в коммуникаторе.
Принимает: Локальный буфер отправки, массив с количеством элементов для отправки каждому процессу, массив с смещениями для каждого блока данных, тип данных отправки, буфер приема для каждого процесса, количество элементов для приема, тип данных приема, ранг процесса-рассылателя (root), коммуникатор.
Возвращает: Ничего.

MPI_Gatherv(&sendbuf, sendcount, sendtype, &recvbuf, recvcounts, displs, recvtype, root, comm):

Действие: Собирает блоки данных от всех процессов на один процесс.
Принимает: Локальный буфер отправки, количество элементов для отправки каждым процессом, тип данных отправки, буфер приема для процесса-корня, массив с количеством элементов для приема, массив с смещениями для каждого блока данных, тип данных приема, ранг процесса-корня, коммуникатор.
Возвращает: Ничего.

MPI_Allgatherv(&sendbuf, sendcount, sendtype, &recvbuf, recvcounts, displs, recvtype, comm):

Действие: Собирает блоки данных от всех процессов на все процессы.
Принимает: Локальный буфер отправки, количество элементов для отправки каждым процессом, тип данных отправки, буфер приема для всех процессов, массив с количеством элементов для приема каждым процессом, массив с смещениями для каждого блока данных, тип данных приема, коммуникатор.
Возвращает: Ничего.

MPI_Barrier(comm):

Действие: Блокирует процессы до тех пор, пока все процессы не достигнут этой точки.
Принимает: Коммуникатор.
Возвращает: Ничего.

MPI_Alltoall(&sendbuf, sendcount, sendtype, &recvbuf, recvcount, recvtype, comm):

Действие: Каждый процесс отправляет данные всем остальным процессам.
Принимает: Локальный буфер отправки, количество элементов для отправки каждому процессу, тип данных отправки, буфер приема для каждого процесса, количество элементов для приема каждым процессом, тип данных приема, коммуникатор.
Возвращает: Ничего.

MPI_Alltoallv(&sendbuf, sendcounts, sdispls, sendtype, &recvbuf, recvcounts, rdispls, recvtype, comm):

Действие: Каждый процесс отправляет блоки данных всем остальным процессам.
Принимает: Локальный буфер отправки, массив с количеством элементов для отправки каждому процессу, массив с смещениями для каждого блока данных для отправки, тип данных отправки, буфер приема для каждого процесса, массив с количеством элементов для приема каждым процессом, массив с смещениями для каждого блока данных для приема, тип данных приема, коммуникатор.
Возвращает: Ничего.

MPI_Scan(&sendbuf, &recvbuf, count, datatype, op, comm):

Действие: Выполняет операцию свертки над данными от каждого процесса и сохраняет результаты в приемном буфере.
Принимает: Локальный буфер отправки, буфер приема, количество элементов, тип данных, операцию свертки, коммуникатор.
Возвращает: Ничего.

MPI_Reduce_scatter(&sendbuf, &recvbuf, recvcounts, datatype, op, comm):

Действие: Сводит данные, отправленные каждым процессом, и рассредотачивает результаты между процессами.
Принимает: Локальный буфер отправки, буфер приема, массив с количеством элементов для приема каждым процессом, тип данных, операцию свертки, коммуникатор.
Возвращает: Ничего.

MPI_File_open(MPI_COMM_WORLD, filename, amode, info, &fh):

Действие: Открывает файл для доступа через коллективный коммуникатор MPI_COMM_WORLD.
Принимает: MPI_COMM_WORLD — коллективный коммуникатор, имя файла, режим доступа, информацию о файле, адрес переменной для дескриптора файла.
Возвращает: Ничего.

MPI_File_close(&fh):

Действие: Закрывает файл.
Принимает: Дескриптор файла.
Возвращает: Ничего.

MPI_File_read(fh, buf, count, datatype, status):

Действие: Читает данные из файла.
Принимает: Дескриптор файла, буфер для чтения, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_write(fh, buf, count, datatype, status):

Действие: Записывает данные в файл.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_seek(fh, offset, whence):

Действие: Устанавливает позицию указателя файла.
Принимает: Дескриптор файла, смещение относительно whence (начала, текущей позиции, конца файла).
Возвращает: Ничего.

MPI_File_get_size(fh, &size):

Действие: Получает размер файла.
Принимает: Дескриптор файла, адрес переменной для размера.
Возвращает: size — размер файла.

MPI_File_get_group(fh, &group):

Действие: Получает группу процессов, ассоциированную с файлом.
Принимает: Дескриптор файла, адрес переменной для группы.
Возвращает: Ничего.

MPI_File_set_info(fh, info):

Действие: Устанавливает информацию о файле.
Принимает: Дескриптор файла, информацию о файле.
Возвращает: Ничего.

MPI_File_get_info(fh, &info):

Действие: Получает информацию о файле.
Принимает: Дескриптор файла, адрес переменной для информации.
Возвращает: Ничего.

MPI_File_get_amode(fh, &amode):

Действие: Получает режим доступа к файлу.
Принимает: Дескриптор файла, адрес переменной для режима доступа.
Возвращает: Ничего.

MPI_File_set_atomicity(fh, flag):

Действие: Устанавливает атомарность операций ввода-вывода для файла.
Принимает: Дескриптор файла, флаг атомарности операций.
Возвращает: Ничего.

MPI_File_get_atomicity(fh, &flag):

Действие: Получает текущее состояние атомарности операций ввода-вывода для файла.
Принимает: Дескриптор файла, адрес переменной для флага атомарности операций.
Возвращает: Ничего.

MPI_File_read_all(fh, buf, count, datatype, status):

Действие: Читает данные из файла для всех процессов.
Принимает: Дескриптор файла, буфер для чтения, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_write_all(fh, buf, count, datatype, status):

Действие: Записывает данные в файл для всех процессов.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_read_at(fh, offset, buf, count, datatype, status):

Действие: Читает данные из файла с указанной позиции.
Принимает: Дескриптор файла, смещение, буфер для чтения, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_write_at(fh, offset, buf, count, datatype, status):

Действие: Записывает данные в файл с указанной позиции.
Принимает: Дескриптор файла, смещение, буфер для записи, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_read_shared(fh, buf, count, datatype, status):

Действие: Читает данные из файла с разделенной памятью.
Принимает: Дескриптор файла, буфер для чтения, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_write_shared(fh, buf, count, datatype, status):

Действие: Записывает данные в файл с разделенной памятью.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_read_ordered(fh, buf, count, datatype, status):

Действие: Читает данные из файла в определенном порядке.
Принимает: Дескриптор файла, буфер для чтения, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_write_ordered(fh, buf, count, datatype, status):

Действие: Записывает данные в файл в определенном порядке.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_read_at_all(fh, offset, buf, count, datatype, status):

Действие: Читает данные из файла с указанной позиции для всех процессов.
Принимает: Дескриптор файла, смещение, буфер для чтения, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_write_at_all(fh, offset, buf, count, datatype, status):

Действие: Записывает данные в файл с указанной позиции для всех процессов.
Принимает: Дескриптор файла, смещение, буфер для записи, количество элементов, тип данных, статус операции.
Возвращает: Ничего.

MPI_File_iread(fh, buf, count, datatype, request):

Действие: Асинхронно читает данные из файла.
Принимает: Дескриптор файла, буфер для чтения, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iwrite(fh, buf, count, datatype, request):

Действие: Асинхронно записывает данные в файл.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iread_at(fh, offset, buf, count, datatype, request):

Действие: Асинхронно читает данные из файла с указанной позиции.
Принимает: Дескриптор файла, смещение, буфер для чтения, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iwrite_at(fh, offset, buf, count, datatype, request):

Действие: Асинхронно записывает данные в файл с указанной позиции.
Принимает: Дескриптор файла, смещение, буфер для записи, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iread_shared(fh, buf, count, datatype, request):

Действие: Асинхронно читает данные из файла с разделенной памятью.
Принимает: Дескриптор файла, буфер для чтения, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iwrite_shared(fh, buf, count, datatype, request):

Действие: Асинхронно записывает данные в файл с разделенной памятью.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iwrite_ordered(fh, buf, count, datatype, request):

Действие: Асинхронно записывает данные в файл в определенном порядке.
Принимает: Дескриптор файла, буфер для записи, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_iwrite_at_all(fh, offset, buf, count, datatype, request):

Действие: Асинхронно записывает данные в файл с указанной позиции для всех процессов.
Принимает: Дескриптор файла, смещение, буфер для записи, количество элементов, тип данных, адрес переменной для запроса.
Возвращает: Ничего.

MPI_File_sync(fh):

Действие: Синхронизирует файл.
Принимает: Дескриптор файла.
Возвращает: Ничего.

MPI_Wait(&request, &status):

Действие: Ожидает завершения асинхронной операции.
Принимает: Адрес переменной для запроса, адрес переменной для статуса.
Возвращает: Ничего.

MPI_Waitall(count, array_of_requests, array_of_statuses):

Действие: Ожидает завершения нескольких асинхронных операций.
Принимает: Количество запросов, массив запросов, массив статусов.
Возвращает: Ничего.

MPI_Waitany(count, array_of_requests, &index, &status):

Действие: Ожидает завершения одной из нескольких асинхронных операций.
Принимает: Количество запросов, массив запросов, адрес переменной для индекса завершенной операции, адрес переменной для статуса.
Возвращает: Ничего.

MPI_Test(&request, &flag, &status):

Действие: Проверяет, завершена ли асинхронная операция.
Принимает: Адрес переменной для запроса, адрес переменной для флага, адрес переменной для статуса.
Возвращает: Ничего.

MPI_Testall(count, array_of_requests, &flag, array_of_statuses):

Действие: Проверяет, завершены ли все асинхронные операции.
Принимает: Количество запросов, массив запросов, адрес переменной для флага, массив статусов.
Возвращает: Ничего.

MPI_Testany(count, array_of_requests, &index, &flag, &status):

Действие: Проверяет, завершена ли хотя бы одна из асинхронных операций.
Принимает: Количество запросов, массив запросов, адрес переменной для индекса завершенной операции, адрес переменной для флага, адрес переменной для статуса.
Возвращает: Ничего.

MPI_Testsome(incount, array_of_requests, &outcount, array_of_indices, array_of_statuses):

Действие: Проверяет, завершены ли некоторые из асинхронных операций.
Принимает: Количество запросов, массив запросов, адрес переменной для количества завершенных операций, массив индексов завершенных операций, массив статусов.
Возвращает: Ничего.

MPI_Probe(source, tag, comm, &status):

Действие: Ожидает появления сообщения, но не забирает его.
Принимает: Ранг отправителя, тег сообщения, коммуникатор, адрес переменной для статуса.
Возвращает: Ничего.

MPI_Cancel(&request):

Действие: Отменяет операцию.
Принимает: Адрес переменной для запроса.
Возвращает: Ничего.

MPI_Request_free(&request):

Действие: Освобождает запрос.
Принимает: Адрес переменной для запроса.
Возвращает: Ничего.

MPI_Test_cancelled(&status, &flag):

Действие: Проверяет, была ли отменена операция.
Принимает: Адрес переменной для статуса, адрес переменной для флага.
Возвращает: Ничего.

MPI_Type_contiguous(count, oldtype, &newtype):

Действие: Создает новый производный тип, который состоит из нескольких блоков одного и того же типа.
Принимает: Количество повторений типа, старый тип, адрес переменной для нового типа.
Возвращает: Ничего.

MPI_Type_vector(count, blocklength, stride, oldtype, &newtype):

Действие: Создает новый производный тип, который состоит из блоков типа, расположенных в памяти через определенные интервалы.
Принимает: Количество повторений типа, длину блока, шаг между блоками, старый тип, адрес переменной для нового типа.
Возвращает: Ничего.

MPI_Type_hvector(count, blocklength, stride, oldtype, &newtype):

Действие: Создает новый производный тип, который состоит из блоков типа, расположенных в памяти через определенные интервалы с учетом смещений в байтах.
Принимает: Количество повторений типа, длину блока, шаг между блоками в байтах, старый тип, адрес переменной для нового типа.
Возвращает: Ничего.

MPI_Type_create_hindexed(count, array_of_blocklengths, array_of_displacements, oldtype, &newtype):

Действие: Создает новый производный тип, который состоит из блоков типа с произвольными смещениями.
Принимает: Количество блоков, массив длин блоков, массив смещений блоков, старый тип, адрес переменной для нового типа.
Возвращает: Ничего.

MPI_Type_create_struct(count, array_of_blocklengths, array_of_displacements, array_of_types, &newtype):

Действие: Создает новый производный тип, который состоит из нескольких блоков разных типов.
Принимает: Количество блоков, массив длин блоков, массив смещений блоков, массив типов блоков, адрес переменной для нового типа.
Возвращает: Ничего.

MPI_Type_commit(&datatype):

Действие: Завершает создание производного типа и делает его доступным для использования.
Принимает: Адрес переменной для типа.
Возвращает: Ничего.

MPI_Type_free(&datatype):

Действие: Освобождает производный тип.
Принимает: Адрес переменной для типа.
Возвращает: Ничего.

MPI_Op_create(&user_fn, commute, &op):

Действие: Создает новую операцию для использования в коллективных операциях.
Принимает: Адрес функции-обработчика, флаг коммутативности операции, адрес переменной для операции.
Возвращает: Ничего.

MPI_Op_free(&op):

Действие: Освобождает операцию.
Принимает: Адрес переменной для операции.
Возвращает: Ничего.

MPI_Errhandler_create(&function, &errhandler):

Действие: Создает новый обработчик ошибок.
Принимает: Адрес функции-обработчика, адрес переменной для обработчика ошибок.
Возвращает: Ничего.

MPI_Errhandler_set(comm, errhandler):

Действие: Устанавливает обработчик ошибок для коммуникатора.
Принимает: Коммуникатор, обработчик ошибок.
Возвращает: Ничего.

MPI_Errhandler_get(comm, &errhandler):

Действие: Получает обработчик ошибок для коммуникатора.
Принимает: Коммуникатор, адрес переменной для обработчика ошибок.
Возвращает: Ничего.

MPI_Errhandler_free(&errhandler):

Действие: Освобождает обработчик ошибок.
Принимает: Адрес переменной для обработчика ошибок.
Возвращает: Ничего.

MPI_Errhandler_create(MPI_ERRORS_ARE_FATAL, &errhandler):

Действие: Создает обработчик ошибок, который устанавливает все ошибки как фатальные.
Принимает: Спецификацию обработчика, адрес переменной для обработчика ошибок.
Возвращает: Ничего.

MPI_Errhandler_create(MPI_ERRORS_RETURN, &errhandler):

Действие: Создает обработчик ошибок, который возвращает ошибки в качестве значения функции.
Принимает: Спецификацию обработчика, адрес переменной для обработчика ошибок.
Возвращает: Ничего.

Это полный список функций стандарта MPI вместе с их кратким описанием.